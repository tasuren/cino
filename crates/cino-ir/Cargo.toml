[package]
name = "cino-ir"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
cino-sema = { path = "../cino-sema" }
cino-syntax = { path = "../cino-syntax" }

[lib]
path = "src/lib.rs"
