# cino 企画書（Draft）

## 1. 背景・課題

現代のソフトウェア開発では、以下の問題が繰り返し発生している。

- ドメインロジックと副作用（I/O、DB、ネットワーク、UI）が混在し、テストや変更が困難
- 設計上は分離しているはずの責務が、運用・時間経過とともに侵食される
- プログラムと仕様書が乖離し、仕様の正当性が担保できない
- 複数言語・複数プラットフォームで同一ドメインロジックを再実装している

これらは「人の規律」ではなく「言語とコンパイラ」によって防ぐ必要がある。

---

## 2. 目的

cino は以下を目的とする。

- **副作用を一切持てないドメイン定義用言語**を提供する
- ドメインロジックを **状態遷移（update）と読み取り（query）** に限定する
- ドメインロジックを **Rust / C / C++ / Python / JavaScript** から安全に利用可能にする
- プログラムそのものから **日本語／英語の仕様書を自動生成**できるようにする

---

## 3. cino の設計方針（重要）

### 3.1 副作用完全禁止

cino プログラムは以下を**構文・型レベルで禁止**する。

- I/O
- 時刻取得
- 乱数生成
- グローバル状態
- 例外スロー
- 外部ライブラリ呼び出し

外界とのやり取りは **Action** を返すことでのみ表現する。

---

### 3.2 中核モデル（最小計算モデル）

cino が提供するドメインモデルは以下に限定される。

#### State

- ドメインの状態
- 不透明（外部言語から直接アクセス不可）

#### Event

- 状態遷移を引き起こす入力
- 外部から注入される（時刻等も Event に含める）

#### Update

```
update : State × Event -> (State, List<Action>)
```

- 状態遷移は必ず純粋関数
- 副作用は Action として“要求”のみ可能

#### Query

```
query : State × Query -> Result
```

- 状態の読み取り専用API
- 派生値・判定・表示用計算を含む
- 副作用なし

#### Action

- 副作用の抽象的要求
- 実行はホスト言語側の責務
- 実行結果は Event として cino に戻す

---

### 3.3 Query の位置づけ

Query は以下を目的とする。

- 状態の内部構造を外部に公開しない
- ドメインが「何を外部に見せるか」を明示的に定義する
- UI表示・業務判断・APIレスポンスを安全に生成する

---

## 4. 型システム・表現力

### 4.1 基本データ型

- 数値（整数・固定小数）
- 文字列
- 真偽値
- List / Map
- Option / Result

### 4.2 代数的データ型

- Enum（タグ付きUnion）
- Record（不変構造体）

### 4.3 パターンマッチ

- Event / Query / Enum に対する exhaustiveness check を必須とする

---

## 5. テスト機構

### 5.1 ユニットテスト

- update / query は完全に決定的
- 同一入力 → 同一出力が保証される
- cino 標準のテストDSLで記述可能

### 5.2 テスト可能性の保証

- 時刻・乱数は Event 注入のためモック不要
- Action は実行せず「生成されたかどうか」だけ検証

---

## 6. 翻訳・仕様書生成

### 6.1 基本方針

- LLM は使用しない
- AST に意味メタデータを埋め込む
- テンプレートベースで文章生成

### 6.2 メタデータ例

- 日本語名 / 英語名
- 説明文
- 制約条件
- 用語カテゴリ（状態 / 操作 / 判定）

### 6.3 出力物

- 日本語仕様書（Markdown / HTML）
- 英語仕様書（Markdown / HTML）
- 差分管理可能（Gitフレンドリー）

---

## 7. ホスト言語連携（FFI）

### 7.1 提供形態（未確定、要議論）

- cino コンパイラが以下を生成
    - バイナリ（static/shared）
    - WASM（検討対象）
- または、インタプリタの実装を提供

### 7.2 外部API

- State は不透明ハンドル
- 利用可能操作：
    - 初期State生成
    - update 呼び出し
    - query 呼び出し

### 7.3 エラー処理

- 例外なし
- すべて Result 型として返却

---

## 8. 想定ユースケース

- 課金・請求ルール
- ワークフロー（申請・承認）
- 権限・ポリシー判定
- 業務ルールエンジン
- 状態機械型ドメイン

---

## 9. 非目標（やらないこと）

- UI構築
- DBアクセス
- 高頻度リアルタイム計算
- 汎用スクリプト言語化

---

## 10. 開発ステップ（AI向け指示）

1. 最小構文（State / Event / update / query）の定義
2. AST 設計
3. 型チェック
4. テストDSL
5. 仕様書ジェネレータ
6. Rust バックエンド実装
7. 他言語FFI拡張

---

## 補足

cino は「便利な言語」ではなく、  
**ドメイン境界を壊せない言語**であることを最優先とする。