# AGENTS.md

このファイルは、`cino` リポジトリで作業する各種 AI ツール向けの共通ガイドラインです。
目的は「複数エージェントが入っても設計と実装を一貫させること」です。

## 1. プロジェクトの目的

- cino は副作用を持たないドメイン定義言語
- 中核は `State / Event / update / query / Action`
- 優先順位は「速度」より「決定性・堅牢性・手軽さ」

## 2. 現在の確定方針

- 実装言語は Rust
- 初期実装はインタプリタ（バイトコードVM前提）
- `update/query` は同期実行（呼び出しスレッドをブロック）
- メモリ管理は呼び出し単位リージョン（GCなし）
- ホスト連携の正準は C ABI
- データ交換の正準シリアライズは CBOR
- 副作用は `Action` 生成のみ（ドメインコードでI/O禁止）

## 3. 参照すべき仕様書

作業前に次を確認すること。

- `PROPOSAL.md`
- `docs/specs/core-language.md`
- `docs/specs/static-semantics.md`
- `docs/specs/runtime-memory-rust.md`
- `docs/specs/ir-codegen-rust.md`
- `docs/specs/host-abi-ffi.md`
- `docs/specs/docgen-spec.md`

## 4. 変更時の原則

- 仕様を先に更新し、次に実装を変更する
- 仕様と矛盾する挙動を実装に入れない
- 不確定事項は TODO として明示する（黙って判断しない）
- 破壊的変更は「何が壊れるか」を必ず記述する

## 5. コーディング原則（Rust）

- パニックを FFI 境界の外へ出さない
- 失敗は構造化エラーで返す
- 非決定的要素（時刻・乱数・外部状態）を評価器に入れない
- `State` は不透明ハンドル越しに扱う

## 6. スレッド・実行モデル

- `update/query` 自体は同期API
- 並行実行はホスト側の責務（スレッド/ワーカーで包む）
- ランタイム内に共有可変グローバル状態を持たない

## 7. シリアライズ方針

- 正準フォーマットは CBOR
- JSON はデバッグ用途に限定（正準扱いしない）
- フォーマットを増やす場合は互換性と用途を仕様へ先に追記する

## 8. ドキュメント言語

- 当面、主要ドキュメントは日本語で管理する
- 英語版は必要時に派生生成する

## 9. 推奨ワークフロー（AI共通）

1. 関連仕様を読む
2. 変更案を最小差分で作る
3. 仕様更新（必要なら）
4. 実装更新
5. 確認（テスト/静的検査）
6. 変更点と未解決事項を簡潔に報告

## 10. 禁止事項

- 仕様未反映のまま意味変更を入れる
- 副作用禁止モデルを破る機能追加
- 暗黙のグローバル状態導入
- 互換性影響を未記載でマージすること

## 11. コミット規約

- コミットメッセージは Conventional Commits に準拠する
- 例: `feat: ...`, `fix: ...`, `docs: ...`, `refactor: ...`
